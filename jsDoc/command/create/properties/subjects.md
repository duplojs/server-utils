Command subjects.

This value is a list of arguments or a list of child commands.

Arguments are read in order and passed to the handler as `args`.
With child commands, the first remaining value selects the child command.

The `subjects` type accepts one of these two shapes, not a mixed list.
