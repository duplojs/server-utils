Controls the behavior when the positional argument is missing.

When `false` or omitted, a missing value adds a command error.
When `true`, a missing value returns `undefined`.

The type system rejects an optional argument declared before a required argument.

@default false
