Rename a path within its parent directory.

Rename the entry to a new name and return the new path.

```ts
{@include file/rename/example.ts[1,9]}
```

@see https://server-utils.duplojs.dev/en/v0/api/file/rename
@namespace SF
